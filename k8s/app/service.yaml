apiVersion: v1
kind: Service
metadata:
  name: food-frezty-service
  namespace: prod
spec:
  type: NodePort

  selector:
    app: food-frezty-app

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080